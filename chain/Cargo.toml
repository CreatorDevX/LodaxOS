[package]
name = "lodaxos-chain"
version = "0.1.0"
edition = "2024"

[dependencies]
lodaxos-system = { path = "../system" }
uefi = { version = "0.37", default-features = false, features = ["alloc"] }
log = "0.4"
